# Task Tracker

Современный и удобный таск-трекер, созданный для эффективного управления задачами. Проект построен на стеке React + TypeScript + Vite с использованием Tailwind CSS для стилизации.

## 🚀 Быстрый старт

Для запуска проекта локально выполните следующие шаги:

### 1. Предварительные требования

Убедитесь, что у вас установлены:
- [Node.js](https://nodejs.org/) (рекомендуется версия 18.x или выше)
- npm (поставляется вместе с Node.js)

### 2. Клонирование репозитория

```bash
git clone <url-вашего-репозитория>
cd task-tracker
```

### 3. Установка зависимостей

Выполните команду для установки всех необходимых пакетов:

```bash
npm install
```

### 4. Запуск в режиме разработки

Запустите сервер разработки:

```bash
npm run dev
```

После запуска проект будет доступен по адресу [http://localhost:5173](http://localhost:5173).

## 🛠 Доступные скрипты

В проекте настроены следующие команды:

- `npm run dev` — запуск сервера разработки с поддержкой Hot Module Replacement (HMR).
- `npm run build` — сборка проекта для продакшена (результат в папке `dist`).
- `npm run lint` — проверка кода линтером (ESLint).
- `npm run preview` — локальный запуск собранного проекта для проверки перед деплоем.

## 🏗 Структура проекта

```text
task-tracker/
├── src/                # Исходный код приложения
│   ├── App.tsx         # Основной компонент приложения
│   ├── main.tsx        # Точка входа
│   ├── index.css       # Глобальные стили (Tailwind)
│   ├── types/          # Определение типов TypeScript
│   └── components/     # Компоненты UI (если есть)
├── public/             # Статические ресурсы
├── vite.config.ts      # Конфигурация Vite
├── tailwind.config.js  # Конфигурация Tailwind CSS
└── tsconfig.json       # Конфигурация TypeScript
```

## 🧰 Технологический стек

- **Frontend:** React 18, TypeScript
- **Стилизация:** Tailwind CSS
- **Сборка:** Vite
- **Иконки:** Lucide React
- **Графики:** Recharts
- **Работа с датами:** date-fns

---
Создано с помощью Cursor.
